include includes/head.pug
<body>
    <button type="button" class="btn btn-outline-primary" onclick="telaCadastro()">Adicionar Usuário</button>
    <button type="button" class="btn btn-outline-dark" onclick="sair()">Sair</button>
    <table class="table">
    <thead class="thead-dark">
        <tr>
        <th scope="col">id</th>
        <th scope="col">Nome</th>
        <th scope="col">Email</th>
        <th scope="col">Deletar</th>
        </tr>
    </thead>
    <tbody>
        each user in users
            tr
                th(scope="row")=user.id
                td=user.nome
                td=user.email
                td(class="btn btn-danger btn-outline-danger", onclick='deleteUser('+'"'+user.id+'"'+')')="Delete"
    </tbody>
    </table>
<body>
script(src="/javascripts/delete.js")